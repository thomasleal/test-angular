import { Routes } from '@angular/router';
import { DogPhotoComponent } from './components/dog-photo/dog-photo.component';

export const routes: Routes = [
    { path: '', component: DogPhotoComponent }
];
